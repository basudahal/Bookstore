<div class="navbar navbar-default">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
          	<h1>Books</h1>
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav navbar-right">
        	<% if current_user %>
		      <%= link_to current_user.name, current_user %> 
		      <%= link_to 'Sign Out', session_path, method: :delete, class: 'btn btn-default btn-sm' %>
		    <% else %>
		      <%= link_to 'Sign Up', signup_path, class: 'inline btn btn-default btn-sm' %>  
		      <%= link_to 'Sign In', new_session_path, class: 'inline btn btn-default btn-sm' %>
		    <% end %>
		    </ul>
        </div>
    </div>
</div>